services:
  - type: web
    name: superkart-backend
    runtime: python
    rootDir: backend_files
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
